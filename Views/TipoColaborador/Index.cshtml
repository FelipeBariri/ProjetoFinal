@model IEnumerable<ProjetoFinalFelipe.Models.TipoColaborador>

@{
    ViewData["Title"] = "Index";
}

<h1>Tipo De Colaborador</h1>

<p>
    <a asp-action="Create">Criar um novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoColaboradorName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoColaboradorName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoColaboradorId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.TipoColaboradorId">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoColaboradorId">Deletar</a>
            </td>
        </tr>
}
    </tbody>
</table>
